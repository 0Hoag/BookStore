server:
  port: 8081
  servlet:
    context-path: /profile

spring:
  application:
    name: profile-service

  neo4j:
    uri: YOUR_URI
    authentication:
      username: YOUR_USERNAME
      password: YOUR_PASSWORD